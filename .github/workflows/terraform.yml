name: Terraform CI/CD

on:
  push:
    branches:
      - '**'

jobs:
  terraform:
    name: Plan and Apply on Changed Terraform Module
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetches all history for all branches and tags

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: '1.0.0'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-region: eu-west-1
          role-to-assume: arn:aws:iam::564407622114:role/github-action-role

      - name: Detect Changed Directories
        id: changes
        run: |
          echo "Fetching list of changed files..."
          git fetch --depth=1 origin ${{ github.base_ref }}
          changed_files=$(git diff --name-only origin/${{ github.base_ref }}...${{ github.sha }} | grep '^terraform/')
          echo "Changed files: $changed_files"
          
          changed_dirs=$(echo "$changed_files" | awk -F'/' '{print $1"/"$2}' | uniq)
          echo "Changed directories: $changed_dirs"
          echo "::set-output name=dir::$(echo $changed_dirs | xargs)"


      - name: Plan and Apply Terraform
        if: steps.changes.outputs.dir != ''
        run: |
          for dir in ${{ steps.changes.outputs.dir }}
          do
            echo "Running Terraform in directory $dir"
            cd $dir
            terraform init
            terraform plan -out=tfplan
            terraform apply -auto-approve tfplan
            cd -
          done
